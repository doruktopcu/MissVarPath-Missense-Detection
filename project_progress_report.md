# Project Progress Report

Sequential lab notebook for MissVARPath / MVP. Each entry is identified by `#N` and describes a single discrete action (data fetch, preprocessing step, model run, ablation, etc.).

The current active model suite is the **11-model fast-baseline** registered in [src/models.py](src/models.py) (`MODEL_SPECS`). See [README.md](README.md) for the model list and [checkpoint.md](checkpoint.md) for the running save-game / roadmap.
